---
# Logger integration. See: https://www.home-assistant.io/integrations/logger/
logger:
  default: error
  logs:
    # log level for HA core
    homeassistant.core: fatal

    homeassistant.loader: error

    # Log level for Text-To-Speech
    homeassistant.components.tts: debug
